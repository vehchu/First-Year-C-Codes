#include <stdio.h>

void initialize(int arr[], int* count);
void display(int arr[], int count);
void display(int arr[], int count);
int searchElem(int arr[], int count, int elem);

int main(){

    int array[MAX]; //an array which can store up to MAX elements
    int count; //stores the actual number of elements

}

void initialize(int arr[], int* count){

    int arr[MAX] = 0;
    int count;

    while(arr[MAX] == 0);
        count = 0;          // count or list is empty
    
}

void display(int arr[], int count){

    int arr[MAX];
    int count, i;

    if(count == 0){
        printf("List is empty");
    }
    else{
        for(i = 0; i < sizeof(arr); i++){
            printf("%d ", arr[i]);
        }
        count = i;
    }

}

int searchElem(int arr[], int count, int elem){

    int arr[MAX];
    int count, elem;



}